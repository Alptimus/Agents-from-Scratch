You must write a python file which uses the proper tool to read a `.docx` file and extract its text content. The file should be named `docx_reader.py` and should contain a function called `read_docx` that takes the file path as an argument and returns the extracted text as a string. You can use the `python-docx` library to accomplish this task. Make sure to include error handling for cases where the file cannot be read or is not a valid `.docx` file. You are given a sample `.docx` in the folder `test_docs` to test your function.